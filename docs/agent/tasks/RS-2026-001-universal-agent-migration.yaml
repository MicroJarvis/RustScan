id: RS-2026-001
status: active
owner: primary-agent
model: current-session
base_commit: f78c3f0
branch: agent/RS-2026-001/universal-agent-migration
worktree: /Users/tfjiang/.codex/worktrees/universal-agent-migration/RustScan

goal: "Introduce the tool-neutral Agent protocol without deleting existing tool files yet."

scope:
  - AGENTS.md
  - docs/agent/
  - docs/index.md
  - .gitignore
  - .codex/
  - docs/superpowers/
  - scripts/agent/
out_of_scope:
  - RustScan business and numerical code
  - user-owned untracked files from the main worktree
  - deletion or cleanup of existing worktrees without owner confirmation

acceptance:
  - "A model can execute a task using only AGENTS.md, docs/agent files, Git, shell, and Cargo."
  - "The main worktree is not modified by this migration."
  - "Existing .codex and historical Superpowers files are classified before removal."

verification:
  - "git status --short"
  - "git diff --check"
  - "rg -n 'Tool-neutral Agent protocol' AGENTS.md"
  - "find docs/agent -maxdepth 3 -type f -print"

artifacts:
  - docs/agent/universal-agent-migration-todo.md
  - docs/agent/README.md
  - docs/agent/task-template.yaml
  - docs/agent/handoff-template.md
  - docs/agent/review-template.md

known_limitations:
  - "Stage 2 removal of .codex and historical Superpowers references is not complete."
  - "scripts/agent checks are not implemented yet."
next_action: "Review this protocol, then classify and remove tool-specific files in a separate step."
